
// Crear una función llamada " ingresarData "
funcion ingresarData
   Adentro pondras el condicional creado en la parte 1


// Crear función llamada " mostrarPerfil "
funcion mostrarPerfil

// A la siguiente estructura pasala a su forma de concatenación
// por el momento solamente guardala en una variable, luego eso será modificada cuando veamos DOM

   variableConcatenada = 
     Aqui crear un "div" con ID "caja"
     Crear un "h3" llamando al indice del array que se encuentre tu nombre
     crear un "p" llamando al indice del array que se encuentre tu edad
     crear una "img" con el atributo "src" que llame al indice del array que se encuentre tu imagen
     crear un "h4" llamando al indice del array que se encuentre tu profesion
     crear un "hr"
     crear un "a" que el texto sea llamando al indice del array que se encuentre tu email   
     cierra div 



// Crear un array con los siguientes datos (los valores pon los tuyos) nombre, edad, email, ruta imagen, profesion 
variable miPerfil = 

function mostrarPerfil(){

   variable =
    "<div id='caja'>"+

    "</div>"




   ;
}