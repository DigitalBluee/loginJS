   
 /* ... casi todo el codigo de la parte 3 ... */

// función mostrarPerfil 

        //  Agregar 2 lineas a la concatenación
        // ...
        botón con id 'agregarInfo' con el texto 'Agregar descripcion'
        Div con id 'info' sin texto
    // cierre div de concatenación

    Agregar el evento de clic al botón con id "agregarInfo" que llame a la función 'fnAgregarInfo'

// cierre función mostrarPerfil

// Agregar la función "fnAgregarInfo" 

    // variable parrafo preguntando al usuario que quiere agregar al perfil
    variable parrafo = preguntar al usuario

    // variable info seleccionando el id "info"
    variable info = seleccionar el id "info"

    // con el método para ver/agregar el html, poner el html dentro de info concatenando la variable parrafo con la etiqueta html que creas conveniente
    info.metodo = codigo html con variable parrafo

// cerrar funcion 


//Faltan  2 datos para concatenar
// Que falta en el boton?
//Que falta para mostrar la info?
        